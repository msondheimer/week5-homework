<div class='col-lg-6'>
  <div class='well'>

  <!-- WRITE THE CORRECT FORM HEADER AND BLOCK USING ERB -->
  <% if false %>
    <% if @comic.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@comic.errors.count, "error") %> prohibited this comic from being saved:</h2>

        <ul>
        <% @comic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <% end %>

    <form action="/comics/create">

    <div class="form-group">
      <!-- DISPLAY A TITLE LABEL -->
      <!-- DISPLAY A TEXT FIELD AND ATTACH THE CLASS OF 'form-control' TO THIS FIELD -->
      <label for="title">Comic Title:</label>
      <input type="text" name="title" class="form-control" id="title" placeholder="Enter comic title here">
    </div>
    <div class="form-group">
      <!-- DISPLAY A DESCRIPTION LABEL -->
      <!-- DISPLAY A TEXT FIELD AND ATTACH THE CLASS OF 'form-control' TO THIS FIELD -->
      <label for="description">Comic description:</label>
      <input type="text" name="description" class="form-control" id="title" placeholder="Enter a description of the comic">
    </div>
    <div class="form-group">
      <!-- DISPLAY AN IMAGE LABEL -->
      <!-- DISPLAY A TEXT FIELD ATTACH THE CLASS OF 'form-control' TO THIS FIELD -->
      <label for="title">Poster URL:</label>
      <input type="text" name="image_url" class="form-control" id="title" placeholder="Enter URL to a comic poster image">
    </div>
    <div class="actions">
      <!-- DISPLAY A SUBMIT BUTTON WITH A CLASS OF 'btn btn-primary' -->
      <button type="submit" class="btn btn-default">Submit</button>
    </div>

  </form>
  </div>
</div>
